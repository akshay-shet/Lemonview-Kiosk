<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- CAMERA FEATURE (optional) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lemonview">

        <!-- LAUNCHER -->
        <activity
            android:name=".IntroActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- AUTH -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- MAIN -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="false" />

        <!-- USER DETAILS -->
        <activity
            android:name=".UserDetailsActivity"
            android:exported="false" />

        <!-- AI -->
        <activity
            android:name=".SkinAnalysisActivity"
            android:exported="false" />

        <activity
            android:name=".SkinResultActivity"
            android:exported="false" />

        <!-- FEATURES -->
        <activity
            android:name=".MakeupAdvisorActivity"
            android:exported="true" />

        <activity
            android:name=".RoutinePlannerActivity"
            android:exported="false" />

        <activity
            android:name=".ShoppingMallActivity"
            android:exported="false" />

        <!-- FILE PROVIDER FOR PDF SHARING -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
